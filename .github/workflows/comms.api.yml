name: Comms API Continuous Integration

on:
  push:
    branches: [main]
    paths:
      - 'src/comms.api/**'
      - 'src/cohabit.application/**'
      - 'test/comms.api.unit.tests/**'
      - 'test/comms.api.integration.tests/**'
      - 'cohabit.slnx'
  pull_request:
    branches: [main]
    paths:
      - 'src/comms.api/**'
      - 'src/cohabit.application/**'
      - 'test/comms.api.unit.tests/**'
      - 'test/comms.api.integration.tests/**'
      - 'cohabit.slnx'

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore dependencies
        run: dotnet restore cohabit.slnx

      - name: Build
        run: dotnet build cohabit.slnx --no-restore --configuration Release

      - name: Run unit tests
        run: >-
          dotnet test test/comms.api.unit.tests/comms.api.unit.tests.csproj
          --no-build
          --no-restore
          --configuration Release
          --logger trx
          --results-directory ./TestResults/unit
          --verbosity normal

      - name: Run integration tests
        run: >-
          dotnet test test/comms.api.integration.tests/comms.api.integration.tests.csproj
          --no-build
          --no-restore
          --configuration Release
          --logger trx
          --results-directory ./TestResults/integration
          --verbosity normal

      - name: Upload unit test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: comms-api-unit-test-results
          path: ./TestResults/unit

      - name: Upload integration test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: comms-api-integration-test-results
          path: ./TestResults/integration

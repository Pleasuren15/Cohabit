name: Cohabit API Continuous Integration

on:
  push:
    branches: [main]
    paths:
      - 'src/cohabit.api/**'
      - 'src/cohabit.application/**'
      - 'test/cohabit.api.unit.tests/**'
      - 'test/cohabit.api.integration.tests/**'
      - 'cohabit.slnx'
  pull_request:
    branches: [main]
    paths:
      - 'src/cohabit.api/**'
      - 'src/cohabit.application/**'
      - 'test/cohabit.api.unit.tests/**'
      - 'test/cohabit.api.integration.tests/**'
      - 'cohabit.slnx'

jobs:
  build-and-unit-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore dependencies
        run: dotnet restore cohabit.slnx

      - name: Build
        run: dotnet build cohabit.slnx --no-restore --configuration Release

      - name: Run unit tests
        run: >-
          dotnet test test/cohabit.api.unit.tests/cohabit.api.unit.tests.csproj
          --no-build
          --no-restore
          --configuration Release
          --logger trx
          --results-directory ./TestResults/unit
          --verbosity normal

      - name: Upload unit test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cohabit-api-unit-test-results
          path: ./TestResults/unit

  integration-tests:
    needs: build-and-unit-test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore dependencies
        run: dotnet restore cohabit.slnx

      - name: Build
        run: dotnet build cohabit.slnx --no-restore --configuration Release

      - name: Run integration tests
        run: >-
          dotnet test test/cohabit.api.integration.tests/cohabit.api.integration.tests.csproj
          --no-build
          --no-restore
          --configuration Release
          --logger trx
          --results-directory ./TestResults/integration
          --verbosity normal

      - name: Upload integration test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cohabit-api-integration-test-results
          path: ./TestResults/integration

  security-scan:
    needs: [build-and-unit-test, integration-tests]
    uses: ./.github/workflows/docker-security-scan.yml
    with:
      image-name: cohabit-api
      dockerfile: src/cohabit.api/Dockerfile

users [color : green, icon : user]{
  id uuid pk
  first_name string
  last_name string
  cellphone string
  email string
  date_of_birth date
  gender char
  bio string
  isOtpVerified boolean
  timestamp datetime
  addressId uuid fk
}

listings [color: blue, icon: house]{
  id uuid pk
  user_id uuid fk
  addressId uuid fk
  description string
  type string
  timestamp datetime
  expires datetime
}

address [color: grey, icon: pin] {
  id uuid pk
  address_line1 string
  address_line2 string
  postal_code string
  province string
}

images [color: yellow, icon: image]{
  id uuid pk
  listing_id uuid fk
  url string
  timestamp date
}

watch_list [color:red, icon: heart] {
  id uuid pk
  user_id uuid
  listing_id string fk
}

users.id < listings.user_id
listings.user_id < images.id
users.id < watch_list.user_id
watch_list.user_id < listings.id
address.id - users.addressId
address.id - listings.addressId


